const oDevConfig = {
    mode: "development"    //REM.: Si on commente cette ligne ce sera "production" par défaut, et donc le bundle de sortie sera illisible/minifié (sur une ligne).
};

module.exports = oDevConfig;